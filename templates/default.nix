{
  go = {
    path = ./go;
    description = "Go template containing go cli and other useful tools";
  };
  poetry = {
    path = ./poetry;
    description = "Python template using poetry with mkShell and venvHook";
  };
  pythonVenv = {
    path = ./pythonVenv;
    description = "Python development shell using mkShell and venvHook";
  };
}
